apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "rt.fullname" . }}-log
  labels:
    {{- include "rt.labels" . | nindent 4 }}
data:
  qlog.json: |-
    {
      "endpoints": [
        "fd://stdout"
      ],
      "formatMode": "json",
      "routings": {
        "DEFAULT": "INFO"
      }
    }
